## Configarr config to sync all German TRaSH guide profiles to Sonarr and Radarr

You need to set your Radarr and Sonarr URLs and API keys in the `config/config.yml` file.

After that, you can run:

```
docker compose up
```
